spring:
  datasource:
    username: root
    password:
    url: jdbc:mariadb://localhost:3306/credhub
  profiles:
    include: dev

flyway:
  locations: classpath:/db/migration/common,classpath:/db/migration/mysql
