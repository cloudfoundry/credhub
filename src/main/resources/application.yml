security:
  oauth2:
    resource:
      id: credhub
  basic:
    enabled: false
  authorization:
    acls:
      enabled: false

spring:
  jpa:
    hibernate:
      ddl-auto: validate

encryption:
  key_creation_enabled: true

