spring:
  datasource:
    username: root
    password: root
    url: jdbc:postgresql://localhost:5432/credhub_test
    tomcat:
      max-active: 20
      max-idle: 10

flyway:
  locations: classpath:/db/migration/postgres
